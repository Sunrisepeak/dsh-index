[pytest]
markers =
    static: descriptor schema and policy checks (no xlings needed)
    isolation: subos isolation compliance
    lifecycle: real install/uninstall (needs xlings + dsh + pnpm)
    verify: functional checks against a booted profile
